# Written only by benchmarks/runner.append_immutable after a real evaluator.
# Do not hand-edit jsonl files in this directory.
# A run id may contain at most one immutable row per arm/task pair.
# A terminal abort is also chained and makes that run id permanently non-resumable.
