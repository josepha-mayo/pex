# STATUS

**Not ready for Devpost submission.** Deadline is 14 Sep 2026, 17:00 PDT.

**Date:** 2026-08-27

PEX is a working supervisor loop with a compact companion window. That is not a finished product and not a submit.

The closed loop now starts in the companion: create a persistent goal (objective, acceptance, constraints/non-goals) and attach it to a worker. Auto-handoff injects the smallest observed bundle into a sibling session on the same project without an explicit POST. STOP extracts structured claims instead of treating silence as "done." Drift with repeated low-information commands can redirect during work; STOP still inspects rather than nags.

Seven distinct Codex-v2 pets are packaged on disk (`pex`, `ledger`, `mesh`, `nudge`, `drift`, `quiet`, `ember`) with `spriteVersionNumber: 2`. Settings can hatch a user pet through `/images/generations` on the configured PEX credentials; text-only Zen fails honestly.

## Ready enough to keep building on

- Isolated Codex App Server inspect: complete task `NOOP`, incomplete task `SEND_NUDGE` on the same `threadId`, Zen `used_llm=true`, no canned `PEX:` prefix. ChatGPT.exe is observe/focus only; same-socket JSON-RPC is still unproven.
- Live four-arm **Codex** (`fourarm_sol.jsonl`) and **Cursor** (`fourarm_solb.jsonl`, this-desktop `cursor_hooks`) rows exist. Manifest is **frozen**. Do not cite leaked 1/5 vs 4/5.
- Live Cursor / OpenCode / Claude Code / **Grok Build** / **OMP ACP** / **Qwen** / **Kimi ACP** / **Hermes plugin** / **Devin org-api** STOP inspect proofs exist under `benchmarks/results/_scratch/` (local machine). Cursor hooks observe without freezing editor work; only destructive shell waits for policy. Restart the bridge after pulling.
- Compact Tauri companion: ~920×700, Settings as its own page, seven v2 pets plus a supervisor catalog (provider + paste any model id; keys stay in `.env`). Selected pet is a transparent desktop overlay. Hover looks then hops; click asks PEX. Cursor hooks observe without freezing editor work; only destructive shell (`rm -rf`, force-push, …) waits for policy. `beforeSubmitPrompt` now honors ASK_HUMAN for a prompt that contradicts an attached constraint (fail-open if the bridge is down). Ask PEX is a read-only supervisor review. ACP send reports failure honestly. STOP extracts claims and checks them against observed pytest/artifact state; contradicted claims get a specific continuation, not a canned `PEX:` nag. Uncertain claims stay silent.
- Local Docker supervisor image builds. AWS AgentCore is **not** deployed (`aws sts` has no credentials here).

## Not ready (blocks submit)

| Track | Gap |
| --- | --- |
| Agent | Recovery adapters through Devin now have live inspect proofs (`used_llm`, `SEND_NUDGE`, no `PEX:`). Hermes uses official `on_session_end` + `pre_llm_call {context}`. Devin polls org-api `exit` then `POST .../messages`. Pi stays Basic with no send. ChatGPT.exe private JSON-RPC is not the isolated `codex app-server`. |
| Design | Companion stills and `docs/demo/companion.webm` now match the current home / Ask PEX / Settings UI. Judged Devpost video still needs a live Cursor or Codex session on top (≤5 min). User hatch exists in Settings; Zen has no image endpoint. |
| Backend | Four-arm manifest is frozen (`benchmarks/manifest.yaml`). Live Codex + this-desktop Cursor rows exist. Do not cite leaked 1/5 vs 4/5. |
| Submit | Architecture PNG exists (`docs/architecture/pex-architecture.png`). No AgentCore AWS deploy (`aws login` still required), no ≤5 min YouTube/Vimeo live-attach pitch, 0/3 builder.aws.com posts published, Devpost Start project is available but **Submit is not done**. |

Never cite leaked 1/5 vs 4/5 under `benchmarks/results/INVALID_LEAKED_RUNS_DO_NOT_USE/`.
