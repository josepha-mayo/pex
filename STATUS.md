# STATUS

**Not ready for Devpost submission.** Deadline is 14 Sep 2026, 17:00 PDT.

**Date:** 2026-08-26

PEX is a working supervisor loop with a compact companion window. That is not a finished product and not a submit.

## Ready enough to keep building on

- Isolated Codex App Server inspect: complete task `NOOP`, incomplete task `SEND_NUDGE` on the same `threadId`, Zen `used_llm=true`, no canned `PEX:` prefix. ChatGPT.exe same-socket is still unproven.
- Live Cursor / OpenCode / Claude Code STOP inspect proofs exist under `benchmarks/results/_scratch/` (local machine). Restart the bridge after pulling; an old process can still be running.
- Compact Tauri companion: 440×720, native Close quits, Codex v2 playback (hover jump, click focuses harness, drag run, look-at-pointer), prompt selected harness, Ask PEX from canonical state.
- Local Docker supervisor image builds. AWS AgentCore is **not** deployed (`aws sts` has no credentials here).

## Not ready (blocks submit)

| Track | Gap |
| --- | --- |
| Agent | Grok Build live inspect not proven. Remaining adapters after the recovery order are shallow or unproven. ChatGPT.exe private JSON-RPC is not the isolated `codex app-server`. |
| Design | Companion is a first real harness-sized window, not a judged demo. PEX-original v2 hatch-pet is only at approved base; rows are not packaged. README still described deleted fake pets until this update. |
| Backend | Frozen four-arm PexBench jsonl does not exist. `four_arm.py` must refuse freeze until live Cursor+Codex arms. Never cite leaked 1/5 vs 4/5. |
| Submit | No AgentCore deploy, no ≤5 min working demo video, 0/3 builder.aws.com posts published, Devpost form not submitted. |

Never cite leaked 1/5 vs 4/5 under `benchmarks/results/INVALID_LEAKED_RUNS_DO_NOT_USE/`.
