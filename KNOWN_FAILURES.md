# Known failures

- **Not submission-ready.** Companion, inspect loop, and adapters are in progress. Do not treat a local Vite window as a Devpost demo.
- Live bridge on `127.0.0.1:7420` may be an old process. Restart after pulling.
- Isolated `codex app-server` ≠ ChatGPT.exe private JSON-RPC.
- Grok Build STOP inspect is not proven. The live test must use one session id (`live-grok-inspect`), not a throwaway start id.
- Hatch-pet Pex: base image exists; row jobs that returned a full 8×11 contact sheet are invalid. Package only after true 1-row strips + look rows 9–10 + `spriteVersionNumber: 2`.
- Do not cite leaked four-arm numbers under `benchmarks/results/INVALID_LEAKED_RUNS_DO_NOT_USE/`.
