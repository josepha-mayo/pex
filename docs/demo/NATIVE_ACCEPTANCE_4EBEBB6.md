# PEX-only native acceptance — package 4ebebb6

Use only the retained NSIS candidate:

`build/release-candidate-4ebebb6/PEX_0.1.0_x64-setup.exe`

Expected SHA-256:

`f92805c7a8f27c901a108fd7488dd0dd5343d4424ff9815538755ca962511635`

The package is unsigned. An unknown-publisher warning is expected.

## Ten-minute acceptance

1. Start PEX and reject the take if startup shows Retry, freezes, or noticeably
   stalls the machine.
2. On Home, require the OpenCode and Codex harness rail, readable Pex companion,
   honest live/checking state, and no clipping or horizontal overflow.
3. Open Inspector, Deck, and each Settings tab. Reject dead controls, stale
   selection, overlapping copy, or secrets displayed in plaintext.
4. Show Pex and Von separately. Require a transparent overlay, restrained motion,
   stable identity, and a fixed close-message control that does not fly with the
   sprite.
5. Dismiss the message while keeping the pet. Hide the pet, restore it from
   Companion Settings, then drag it once. Reject accidental activation or a
   background rectangle.
6. In Supervisor Settings, require Zen,
   `muse-spark-1.3-contributor-free`, a saved-credential indication without the
   key, and the default three-review cap. A saved key is not proof it is valid.
7. Connect only the dedicated loopback OpenCode demo server. Require the intended
   session and workspace identity before attaching the prepared goal.
8. Run the recovery and quiet cases from `REHEARSAL_CARD.md`. Recovery must send
   exactly one evidence-grounded follow-up to the same session and verify the
   exact final artifact. Quiet completion must choose model-backed NOOP and send
   zero follow-ups.
9. During the journey, reject unbounded CPU/memory growth or an unresponsive
   window. The latest 60-second full-tree sample moved only 324.55-325.04 MB
   private memory across ten processes; this is a reference, not a waiver.
10. Close PEX normally. Require the bridge listener and owned helpers to exit.

Do not show a credential, private session, hidden evaluator, stale benchmark
score, or claim an AWS AgentCore deployment. Retain every failed take.
