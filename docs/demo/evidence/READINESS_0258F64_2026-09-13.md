# Current release readiness boundaries — 13 September 2026

Source at check: clean `0258f6410f718d22f10bc7dd5b2053fa9d23cac8`,
equal to `origin/main`. Product code remains the verified package source
`1cd42c8`; later commits contain evidence only.

## Strands and AgentCore implementation

The focused local implementation suite passed:

```text
243 passed in 25.61s
```

It covered AgentCore client, pipeline, runtime, preflight, Strands runtime, and
the Strands supervisor integration. The full non-live source gate also remains
4,532 passed with 16 skips and 18 explicitly deselected live tests.

The current read-only AgentCore preflight exited 1 by design and reported:

- preferred region `eu-north-1`;
- `aws_cli: true`, but `aws_authenticated: false`;
- current AgentCore CLI absent;
- AWS CDK absent;
- Docker CLI present, but its engine unavailable;
- no verified ARM64 image;
- no configured Runtime ARN;
- `deployable: false` and `invokable: false`.

No AWS resource was created, deployed, invoked, or billed. Local passing tests
prove implementation behavior under controlled contracts; they are not a live
AgentCore Runtime proof.

## Formal benchmark readiness

Fresh `benchmarks/four_arm.py readiness` exited zero as a diagnostic but
correctly reported:

- `manifest_frozen: false`;
- `coherent_runs: []`;
- `can_freeze: false`.

The binding blockers include the absent OS-isolated hidden-evaluator execution
backend, incomplete immutable raw logs and natural-task boundary, inconsistent
historical row configuration, and Cursor's observe-only hook rather than
synchronous same-session treatment. The incomplete historical rows cannot be
turned into a valid uplift score.

The fresh OpenCode and Codex recovery/quiet pairs are behavioral acceptance
evidence. They must not be relabeled as the four-arm benchmark.

## Submission meaning

The local Windows MVP now has a verified installer, exactly two companions,
vault-backed Zen BYOK, a real Strands supervisor, and fresh OpenCode plus Codex
recovery/quiet evidence. AgentCore deployment and formal comparative benchmark
remain explicit omissions. The demo and submission must present those
boundaries truthfully unless new live evidence is produced.
