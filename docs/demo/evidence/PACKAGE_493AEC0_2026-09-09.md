# Rebuilt package checkpoint — 9 September 2026

Product source: `493aec07649ef151124c6d33d85f5374ff6ad096`.

`npm run tauri -- build` completed with exit 0, rebuilding all three sidecars,
the React frontend, native shell, MSI and NSIS installers. Rust compilation used
`CARGO_BUILD_JOBS=2`. The unused `bridge_port_state_at` warning remains.

`npm run verify:package -- --receipt C:\Users\JosephMayo\Projects\pex\build\pex-package-receipt-493aec0.json`
completed with exit 0. MSI and NSIS extraction, embedded payload equivalence
(allowing only the expected desktop bundle marker), source fingerprint and
eight-pet inventory checks passed. Receipt `release_ready:true` means this
package gate only; it does not establish product/submission readiness.

SHA-256:

| Artifact | Hash |
| --- | --- |
| Canonical desktop executable | `373cfed1a99e6976b1d92d5806285a3191722f7aad7a15dda1953d5c2b19e03d` |
| MSI | `389a374232c0ce42cefc50cd02761ec0b26a7f11a843af4ebf67e5dd2fee82ad` |
| NSIS | `71f995ee43762ab86234d042d5f4434ee3343ac41291469a78e4186f29f3499e` |
| Embedded bridge (both installers) | `725988456df631bd3a26c514f06b5a0a81813bd3386532a4be608a54a25401c4` |

This replaces the mixed frontend/1985caa-backend preview at the canonical build
paths. It contains the white-canvas startup-call removal, respected pet sizing,
slower animation, stable close anchor, quieter picker, workspace Home layout,
nonblocking adapter probes and bounded uncompressed event stream.

No desktop UI was opened and no installer was installed. Frozen bridge execution
was restricted to the standalone `--verify-bundle` inventory path, which returns
before app initialization, server startup or parent-watchdog registration.
The quarantined native smoke script was not used. No model calls, cloud
deployments or computer input occurred.

Still open: resolve the native-testing incident/safe verification path, visibly
confirm transparency and hide/dismiss behavior, measure runtime resource use,
verify the complete BYOK/worker/goal flow and finish an honest comparative
benchmark. Do not present this build as a finished hackathon submission.
