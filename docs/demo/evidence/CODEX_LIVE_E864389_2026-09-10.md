# Clean-source Codex and Strands behavioral checks

Source: `e86438942b4e4cd1156456627c014fd284aac1b6`, clean before and after
each run. These are controlled behavioral checks, not comparative benchmark
scores, native desktop attachment proof, or submission approval.

## Verified results

- No-turn preflight: signed-in account type `chatgpt`; requested
  `gpt-5.3-codex-spark` available. No fallback model.
- `test_live_codex_stop_inspects_with_strands`: **1 passed in 79.19s**,
  exit 0; receipt `validated`, `evidence_supported_noop`, one worker turn.
- `test_live_codex_incomplete_stop_sends_specific_continue`: **1 passed in
  193.45s**, exit 0; receipt `validated`, `same_thread_intervention_outcome`,
  two turns in the same owned App Server thread/process. PEX identified the
  missing artifact, delivered a specific correction, verified `report.txt`
  exactly `shipped`, recorded `goal_evidence_supported` / `helped: true`, and
  returned a final model-backed NOOP.

Both use the saved OS-vault Zen Muse Spark 1.3 Contributor Free route through
local Strands, three supervisor dispatches maximum per session, a workspace-write
worker sandbox with network disabled, and explicitly authorized existing Codex
quota. No key was printed, no paid fallback or AWS deployment was used, and
only each fixture's owned App Server handle was closed. The Codex desktop UI
was not automated, stopped or restarted.

Commands (ignored local runner, preserving older scratch receipts):

```powershell
.venv/Scripts/python.exe build/codex_live_evening_20260910.py quiet
.venv/Scripts/python.exe build/codex_live_evening_20260910.py recovery
```

The runner invokes the named tests in `tests/contract/test_live_codex_pump.py`;
each run retains its own JUnit XML, proof and previous scratch proof.

| Local receipt | SHA-256 |
| --- | --- |
| `build/codex-evening-20260910-quiet/codex_inspect_proof.json` | `377071578ba55f348d363ff437be084587d8ea5c58e0411f9e6bc394f8649342` |
| `build/codex-evening-20260910-recovery/codex_incomplete_proof.json` | `b7e2e819f732eb69376f0f28df66963cc1adb9224462b12d87131cd6eb58c2eb` |

Raw receipts remain local pending privacy review. Historical OpenCode free-tier
failure is not overwritten by these Codex passes. The new provider-limit fix
has offline regression evidence; rebuilt native verification remains necessary.
