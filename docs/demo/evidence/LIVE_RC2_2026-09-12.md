# Live RC2 supervision checks — 12 September 2026

Source `44d488c41644b73c6af60b3d5c4b576103dc58b3`, clean and unchanged throughout
both runs. Product code matches installed candidate `103f4ec`; later commits
only change documentation. These calls used real OpenCode HTTP/SSE events and
the production PEX pipeline with Strands, the saved Zen vault credential,
`muse-spark-1.3-contributor-free` supervisor and `ling-3.0-flash-fin-free`
worker. Both models were listed free on the official Zen page immediately
before execution. Public disposable tasks only; no paid fallback or AWS calls.

## Results

| Case | Initial output | PEX follow-ups | Final output | Strict case verdict |
| --- | --- | ---: | --- | --- |
| Controlled recovery | Stage one had CRLF; final absent | 1 | Both files exact | FAIL: initial stage was not exact |
| Deduplicate | Correct | 0 | Correct, input preserved | PASS |
| Quantity sum | Correct | 0 | Correct, input preserved | PASS |
| Boolean flag | Two CRLF newlines instead of one LF | 1 | Exact, input preserved | FAIL: not initially correct |

Both incorrect outputs were corrected, and both initially correct outputs were
left alone. Do not turn this into a 4/4 benchmark score: the controlled recovery
precondition failed and only two quiet cases were eligible as initially correct.
The original receipts and failed verdicts are retained unchanged.

Recovery: exact final files, one same-session SEND_NUDGE, completed semantic
reviews, causal recovery proof true, 157 settled events, 20,256 input tokens,
1,990 output tokens and six model calls. The runner waited its full 360-second
window because its fixed initial-stage criterion had failed. That duration is
not the intervention latency. Audit evidence shows an independent verifier
approved the newline/missing-file correction and its outcome recorded helped.

Deduplicate: 81.84 seconds, two NOOP decisions, four model calls, 13,759 input
and 1,631 output tokens. Quantity: 61.19 seconds, one NOOP, two model calls,
6,761 input and 732 output tokens. Flag: 150.73 seconds, one correction and NOOP
reviews, seven model calls, 22,983 input and 2,544 output tokens. Every case
settled its observed events. Token counts describe recorded supervisor usage.

## Reproduction and evidence

Commands from repository root:

```powershell
.\.venv\Scripts\python.exe scripts\opencode_recovery_once.py --run-name live-recovery-rc2-20260912
.\.venv\Scripts\python.exe scripts\opencode_quiet_ten.py --run-name live-quiet-rc2-20260912 --case-count 3
```

Both commands exited 1 for their strict case conditions, with no runner
exception. Both owned OpenCode servers exited; port 4098 had no remaining
listener afterward. Evidence lives under the named ignored build directories:
summary.json, receipt.json, journal.json, interventions.json and worker messages.
Keep those files; never overwrite a failed run with a rerun.

This is live source-pipeline evidence, not installed-desktop supervision or a
comparative benchmark. A visible installed-app workflow and a fair comparator
remain separate verification tasks. No new RC2 release was published during
these checks; release notes are prepared.
