# PEX identity and supervisor-receipt candidate `5e2f268` — 12 September 2026

This candidate advances the accepted two-pet MVP without changing the public
`v0.1.0-rc1` artifacts. It replaces the old generic PEX wordmark with a
transparent cat-and-orbit identity that has a neutral rounded visor rather than
a heart-shaped face panel. The same identity is used in the native platform
icons, application top bar and startup recovery screen.

The Inspector now exposes a compact, evidence-backed supervisor inference
receipt from the bridge snapshot: provider, model, call count and input/output
token totals when a real model call occurred, or an explicit statement that a
review was deterministic/budget-limited. It does not invent cost or imply that
every observed event caused an inference.

## Exact source and artifacts

- Product source: `5e2f26810df4186f1099e5963557dd0a2c6cea00`.
- Source and `origin/main` matched before packaging.
- Release-input SHA-256:
  `da5928272f7b65ce36f07095b416f74545bb32d29ddedf518f43cf14b174bcf6`.
- Sidecar-input SHA-256:
  `c2f3b66d3a5ce6fc45e81dd486ceab2501fa4dbb5ba8a4da5e3851bb5fc66321`.
- Canonical desktop SHA-256:
  `7050fccf176da1248efcba26bf49809cf26b27047626b96387ee997aca9b9e78`.
- Packaged bridge SHA-256:
  `717c11b9032a0b439bb9b7f721d6ff0b8296031ac0535b74c3ae61458b887535`.
- Package receipt: `build/pex-package-receipt-5e2f268.json`.
- Package receipt SHA-256:
  `fc0d8fd3187de5569cfc2bfe9fe61156cd88b7ed2f981750a263bf89801381f9`.
- Verifier: `release_ready:true`, zero blockers; MSI and NSIS inventories both
  verified.

Retained installers:

| Installer | Bytes | SHA-256 |
| --- | ---: | --- |
| `build/release-candidate-5e2f268/PEX_0.1.0_x64-setup.exe` | 101,753,296 | `e9cae6be4052042539576e9e13818ce1d69f406ed1d8f90d3cb3cca2a6ba88fd` |
| `build/release-candidate-5e2f268/PEX_0.1.0_x64_en-US.msi` | 114,597,912 | `2b667e517836d163fdf08269a9bb04d6190ed8c766614e1df6e9ea39033e4f1f` |

## Verification

- Desktop suite: 295 passed, one intentional Windows symlink skip, zero
  failures (296 total).
- Production TypeScript/Vite build: passed; 73 modules transformed.
- Focused backend/provider/UI-snapshot gate: 68 passed; Ruff passed.
- Broad agent gate: 442 passed in 99.65 seconds. This covered Strands structured
  decisions, the independent verifier, AgentCore client/pipeline/runtime,
  providers and BYOK, Codex correction, supervisor settings, pet snapshot and
  event processing.
- Package verifier: exit 0, `release_ready:true`, zero blockers.
- Authenticated packaged-settings smoke: identity verified, provider `zen`,
  first catalog model `muse-spark-1.3-contributor-free`, dispatch cap three,
  cloud reasoning disabled, worker attachment disabled and zero provider calls.
  Receipt: `build/packaged-settings-smoke-5e2f268.json`; SHA-256
  `2cc489d565f9bae2a2e27538f08da1c45e8c58aa55e43c76d6a03ead398aa3b6`.
- Exact frozen packaged bridge: 3/3 lifetime, payload-reaping and standalone
  two-pet bundle contracts passed in 8.57 seconds.
- Browser render at the native 920 by 700 client size had no horizontal
  overflow. Home showed the explicit OpenCode/Codex worker rail, the Pex
  companion workspace, bounded navigation and the new identity.

The first verifier invocation ran from a shell without the pinned Rust path and
stopped safely at source preflight with `rustc ENOENT`; it created no receipt.
The invocation with Rust 1.97.1 explicitly active passed and produced the
exclusive receipt above.

## Honest claim boundary

`release_ready:true` proves package integrity only. This candidate is not yet a
new public release and has not received a fresh installed-native interaction
sweep. The public RC1 remains pinned to `49385f2` and must not be overwritten
without explicit release authorization.

AgentCore remains implemented and locally protocol-tested, not AWS-deployed.
The formal PexBench matrix remains unfrozen (`can_freeze:false`) and has no
publishable uplift score. Existing live same-source OpenCode recovery and quiet
completion evidence remains the strongest behavioral proof; this package gate
does not relabel offline tests as a fresh live benchmark.
