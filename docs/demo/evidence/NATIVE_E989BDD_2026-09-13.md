# Installed e989bdd live acceptance — recovery failed

Product source: e989bdd1fe2925f57a8aa72992eb5676fb30f72e. Installed
desktop hash: f49677e1950ded64ead0abe0378d7124143a2d65835b33e88ee585fc9ab9ba36.
This is native installed-app evidence, separate from the earlier source-only
ten quiet controls. Do not classify this run as a quiet-control pass.

## Procedure and retained evidence

Used only the PEX window for native input. Connected the installed app to an
owned OpenCode server at loopback port 4097, then created and attached the goal
through Inspector. The worker was real OpenCode Ling free; supervision used the
saved Zen Muse Contributor Free vault configuration and real Strands.
No credential was printed or re-entered, no AWS deployment was made, and no
unrelated app or process was stopped.

Native session: `opencode:ses_f6752d433ffephhNiErWoLOrrT`.
Goal: `goal_518c07d9e95441b38f72e715c374df05`.
Local raw fixture: `build/native-e989bdd-progress-20260913/`.
Runner: `build/native_e989bdd_progress.py` (owned server only, immutable samples,
retained profiles, no separate source Pipeline and no manual worker correction).

Public acceptance criterion supplied to both worker and PEX:

> status.txt contains exactly ready followed by one LF newline (U+000A), no CR

## Observed results

- Native connection and persistent goal creation/attachment worked.
- Fresh worker text appeared in Home and Inspector. This confirms the prior
  live-progress binding repair on the installed app.
- Worker wrote `readx\n`, not `ready\n`, and falsely claimed success while its
  own byte report contained 120 (`x`). The file was not manually corrected.
- Strands inspected the file and proposed an intervention, but the independent
  verifier timed out after approximately 25 seconds. The resulting NOOP had
  diagnosis `strands_structured_decision:independent_verifier_rejected` and
  evidence `independent_verifier:timeout`. No correction was sent.
- Inspector truthfully showed **Review incomplete** and uncertain completion.
- Native Ask PEX, “did the eval actually finish?”, returned “Completion is
  uncertain; PEX has no current-intent supported STOP evidence.” This passed
  the no-false-completion check, not successful recovery or deep diagnosis.
- More than eight minutes after the initial prompt, the artifact remained
  wrong and the intervention table still contained only that rejected review.

## Causal findings / next work

The deterministic acceptance parser recognizes “followed by one newline” but
returns no file requirement for the explicit LF/U+000A/no-CR wording above.
Read-only reproduction confirmed that difference. Extend only unambiguous LF
syntax with strict byte comparisons and ambiguity regressions; do not loosen
exact-content requirements into substring checks or bypass independent review.

Separately, before the selected worker emitted its first text, Home displayed
an older worker's correction. Home's pet status is fleet-wide while its heading
is selected-session-specific. Audit this presentation boundary; do not rewrite
historical database events to conceal it.

## Resource sample

One 30.02-second sample of the installed PEX root and its descendants found ten
processes, 744.04 MiB summed working set, 389.08 MiB private memory, and 27.59%
of one CPU core. Shared working sets can double-count pages. This was after a
live model invocation while an independent offline test suite was also running.
It is neither an idle baseline nor a long-duration leak/whole-PC stability pass.

Owned fixture cleanup completed with `owned_server_exited: true` and
`profiles_retained: true`. PEX itself was closed normally through its own title
bar; native window enumeration returned no remaining PEX windows.
Worker-message SHA256: 379a67f3f85abac0c0250efd77a63580ea901b796af7a39d7560f970e3177b6d.
Fixture runner SHA256: eb20652055aa51e30ad194a4ee145fc8bb0d3cb99c82cd46625826cd266f6545.

Full offline regression completed against unchanged e989bdd product source:
**4480 passed, 1 failed, 16 skipped, 18 deselected in 1222.11 seconds**.
The failure was test-only `test_windows_source_setup_executes_only_ordered_fake_commands`:
its repeated PowerShell Add-Content calls encountered a locked temporary
commands.tsv file. Original failed XML remains `build/offline-e989bdd.xml`,
SHA256 6d1b8cac2033ed95ca0892946fcc21edb29b63a88ffaa0f6df9ac2a588584e1f.

## Source repairs after this failed native run

- Added explicit LF/U+000A/no-CR grammar, still full-match and byte-exact.
  Regression first reproduced **20 failures and 5 passes**, then passed after
  repair. Unsupported CRLF/contradictory/trailing prose remains uncertain.
- Independent verifier now defaults to a bounded 45 seconds rather than 25.
  Same call/token limits, no automatic retry, same fail-closed verdict and
  unchanged outer 70-second dispatch deadline. Overrides remain bounded.
  This is a latency mitigation, not proof that provider timeouts are eliminated.
- Setup test records fake invocations in memory and emits them with its result,
  retaining command order/fail-fast assertions without repeated log-file writes.
- Home's selected-worker bubble now uses that worker's evidence and own review;
  fleet-wide counts remain separate. No historical events are rewritten.

Focused backend suite: **188 passed in 9.58 seconds** including exact file
verification, Strands approval/rejection, deadline bounds and source setup.
Receipt: `build/native-findings-focused-20260913.xml`. These changes were made
after the full offline run; they require fresh live/package acceptance.

Additional cross-layer verification: **140 passed in 33.91 seconds** covering
Strands, AgentCore Pipeline, OpenCode progress ingestion, pet snapshots, exact
LF and setup. Desktop: **301 passed, one Windows symlink-permission skip**.
TypeScript/Vite production build and Ruff passed. A stale frontend source-shape
assertion was updated to require the new selected-session binding rather than
the old fleet-wide input; behavior tests cover cross-session isolation, stale
state, pause, incomplete review, unavailable review and review-budget exhaustion.
