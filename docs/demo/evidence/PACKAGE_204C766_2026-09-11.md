# PEX capped two-pet package candidate — 11 September 2026

Package source: `204c76601ae5cf7392c84f5ff779c3a0165b7e9c`.
The product change is `7bf591c57f3b88c6dc02d21ffec9e0db0b2f9f08`;
the intervening commit updates only status documentation.

## Package result

The release sidecars were rebuilt from clean source after the first preflight
correctly rejected their stale predecessor. The clean preflight then reported
`source_ready: true` with exactly Pex and Von, current sidecars, verified pinned
toolchains and no blockers. The full Tauri build completed with exit code 0.

The package verifier extracted MSI and NSIS, compared the desktop binary, Cursor
helpers and complete bridge runtime, and reported `release_ready: true`, no
blockers and 2,375 matching runtime files.

| Installer | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,743,048 | `8c2a13e086722cf991957cbcf927988bd1b322f50e41c8c31f4b0f6a8bcc5dd2` |
| `PEX_0.1.0_x64-setup.exe` | 98,910,125 | `262907d849e608a95b1ba2ae3c795c33c0a9fa48c8465a43aa7232922ba4f4bb` |

The local machine-readable receipt is
`build/package-204c766-20260911-verified.json`, SHA-256
`16375f7afc60df94fdc5bec230f5b61bac6c7e1ebdc96124576786369cbcc2ce`.
The first verifier invocation stopped before inspecting installers because its
shell omitted the pinned Rust path (`rustc ENOENT`); the corrected invocation
used the verified toolchain and produced the receipt above.

The installers are unsigned development candidates, not a published release.

## Product change and bounded gates

Fresh installs now default to three durable semantic dispatch reservations per
worker session. This caps BYOK model use and enables paced trajectory review;
an explicit Settings or environment override remains available.

- Ruff: passed.
- Affected pipeline/settings tests: 126 passed.
- Provider/source/settings tests: 156 passed, one intentional provider skip.
- Desktop: 290 passed, one platform-specific skip.
- Passive-resource backend checks: 10 passed.
- Source setup contracts after documentation updates: 8 passed.

## Claim boundary

This package does **not** yet prove a fresh-user install, native verification of
the newest Ask/cancellation/cap behavior, long-running foreground stability,
AgentCore deployment, a frozen comparative benchmark score, a recorded demo or
a Devpost submission. Those remain explicit shipping gates.
