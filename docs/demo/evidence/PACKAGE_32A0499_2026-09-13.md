# Installed 32a0499 acceptance — 13 September 2026

Source: `32a0499e4af7f4950c3bd3d27cdf5ee1762aa6b4`.
This fixes the pet Hide control to the existing positioned stage, so dismissing
the independently closable message no longer changes Hide's location.

## Build and package

- Desktop: 299 passed, one intentional Windows symlink skip.
- TypeScript/Vite: passed, 74 modules. `git diff --check`: passed.
- Native diagnostic and full MSI/NSIS builds passed. The CSS-only build reused
  the unchanged bridge runtime, validated by release preflight and full package
  inventory verification; it did not rebuild the Python sidecar unnecessarily.
- First extraction receipt `build/pex-package-receipt-32a0499.json` failed only
  temporary cleanup with EPERM. It is retained, and temporary artifacts may
  remain. No permissions were changed or diagnostic files manually deleted.
- One retry on unchanged installer bytes passed with `release_ready:true`,
  zero blockers: `build/pex-package-receipt-32a0499-retry.json`, SHA-256
  `db1f82d9730306f54254d99ae97a2e07073e21893d9be6842f328271e0372375`.

Retained artifacts: `build/release-candidate-32a0499/`.

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| PEX_0.1.0_x64-setup.exe | 101672562 | `1e60c0ea6ac3f1da5a75b5764778c01400c5c0e40f27b100cafdaa87e4a25688` |
| PEX_0.1.0_x64_en-US.msi | 114479128 | `22c379d9318ad94b1fce6c4e5cdeb283908f94546ea8a231c17b70c53433b564` |

Silent NSIS install exited 0. Installed desktop SHA-256 matched the extracted
NSIS receipt: `88199799dc2079d617ccbb5b23aa05a589d9a9d3fefa4825ab3b7104171d2f1a`.
These are local unsigned candidates, not a new public release.

## Native checks

The initial diagnostic executable (`f5640c4cc1a0c6e3584c311e4092ffd4bb3b9dc7b8a35f8c9081c4f8eaf5e58a`)
passed message-dismissal and stationary Hide checks. The subsequently bundled
desktop has different bytes; diagnostic evidence is not substituted for it.

The installed package was then launched and checked separately:

- Home starts without a bridge error; flat mint logo is visible.
- Companion settings show exactly Pex and Von, selected Von at scale 1.00.
- Show desktop pet immediately displays Von with transparent surroundings.
- Message dismissal leaves Von visible and Hide fixed at approximately
  `(248, 133)` in the pet window, before and after dismissal.
- Hide removes the pet and unchecks Show desktop pet, restoring the initial
  preference. No unrelated application was closed.
- The first installed launch closed normally; a subsequent process check found
  no PEX desktop or bridge remaining. A later launch is used for live testing.

No long-duration freeze/leak claim follows from these bounded interactions.
Earlier resource measurements and live worker proofs retain their own source
and package boundaries; see the previous package and live-evidence reports.
