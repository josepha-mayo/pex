# PEX two-pet MVP package candidate — 11 September 2026

Package source: `9668bcc2afb5a080cfa0936d778c2f8c641544fa`.

This candidate narrows the runtime and release tooling to the two-pet submission
story. It removes unreachable frontend hatch-generation code, prevents the bridge
from importing or initializing the retired hatch/image stack at startup, removes
the retired eight-pet release validator, and excludes/strips the three retired
hatch implementation files from the frozen bridge runtime. Pex and Von remain the
only bundled pets. The final Rust release build completed without the prior
test-helper dead-code warning.

## Verification

- Submission spine: 259 passed in 149.89 seconds. Coverage includes goal lifecycle,
  OpenCode recovery logic, supervisor settings and providers, Strands decisions,
  AgentCore pipeline, bridge APIs, and the two-pet boundary. No provider or AWS call.
- Complete desktop contracts at final source: 290 passed, one intentional Windows
  symlink-permission skip.
- Rust: 19/19 tests passed; release-mode `cargo check` passed without warnings.
- TypeScript/Vite production build: passed, 69 modules transformed.
- Tauri release build: passed; MSI and NSIS completed.
- Package verifier: `release_ready: true`, zero blockers, source bound exactly to
  `9668bcc2afb5a080cfa0936d778c2f8c641544fa`.
- Frozen runtime: 2,372 files and zero paths matching
  `pex_bridge/pets/{hatch,hatch_store,imagegen}`.
- Three sequential isolated packaged-bridge restart smokes passed. Each proved
  HMAC identity, authenticated supervisor read, dispatch cap `3`, and first Zen
  catalog entry `muse-spark-1.3-contributor-free`. Cloud reasoning and worker
  attachment were disabled; provider calls were `0`.

The reusable smoke command is:

```text
.venv\Scripts\python.exe scripts\smoke_packaged_bridge.py --exe apps\desktop\src-tauri\binaries\pex-bridge-runtime\pex-bridge.exe
```

## Immutable artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,648,792 | `6be4090ab5d4969dd923addbea31234bbb8d4579c32a50e7f7d4c6f5e20f51a2` |
| `PEX_0.1.0_x64-setup.exe` | 98,837,415 | `6bf4154a058ddb6c54be71fd90084953447bb8c45850dca5fd3516cb6ef80db8` |
| `pex-bridge.exe` | 34,459,441 | `962b2ebee197865873d1e9b37c09e0e0886e8902df6be350d29f8029378c8ad1` |
| `build/pex-package-receipt-9668bcc.json` | local verifier receipt | `27837dcb8263b4f4f32e1c6109f88ad1790d63ffe3fa83e5130c1fecc0df8b80` |

The installers are unsigned candidates. Package integrity and headless bridge
startup are accepted. Native UI/UX, foreground/active-worker stability, the final
bounded Zen/OpenCode corrective and quiet journeys, recording, public release,
and submission remain open. No native PEX window, browser, live model, worker, or
AWS resource ran during this package gate.
