# 927106a installed/live checks and exact-objective failure

Tested product source: `927106a737beae83d3cff8fcf0798c2fd9932bf8`.
All live runs below held tracked source unchanged. No paid-provider switch,
AWS deployment, external publication, or submission occurred. Native input was
confined to PEX; unrelated Codex tasks were not interrupted.

## Real OpenCode recovery

Controlled two-phase recovery passed in 67.50 seconds: first stop had exactly
stage-one and no final file, with zero prior follow-ups. PEX sent one independently
approved same-session correction, observed both exact files, recorded helped=true,
and then chose NOOP. 148 observed events settled; five supervisor calls consumed
16908 input and 1616 output tokens. Real worker: Ling free; supervisor: saved
Zen Muse Contributor Free through Strands. Owned server exited, profiles retained.
This is a controlled behavioral diagnostic, not a comparative experiment.

Receipt: `build/recovery-927106a-20260913/summary.json`, SHA256
`8446b2a535460a27f847f00a8ddb7c686fbfc01a8dfe375b651dcf11cf331c17`.

## Matching package installed

Full bridge/frontend/Rust/MSI/NSIS build passed. Three frozen lifetime tests
passed in 11.61 seconds. Package verification returned release_ready=true and
zero blockers. Installer exited zero; installed EXE matched extracted NSIS EXE.

- Retained installers: `build/release-candidate-927106a/`.
- NSIS SHA256 `dbb7a7e28d57d7a0b1adee5ec2f6c4ddfcbb8a5b5298a6a76f8fdc975c596e75`.
- MSI SHA256 `86b19f930cea9794971b1876e9975386cce67fade6502a185ed3b38a507ff6c6`.
- Installed EXE SHA256 `8896b98c0054dea0b4db8c244b71e6d30a4e0fe7ae7db3dc77275f36db629860`.
- Package receipt SHA256 `09c8e513c1b15e4f4f7c85b98dcfa5ef90edc4a2f7406842a525335a2cdc61b5`.

## Native OpenCode / saved BYOK / Strands

Connected the fresh owned server at 4097 in Settings. Explicitly selected the
fresh OpenCode session (Home initially selected the Codex placeholder), entered
and attached its goal through the UI. No selection drift occurred during entry.
This does not independently recreate the automatic-selection reordering case;
that repair also has source-contract coverage.

Session: `opencode:ses_f67230a26ffeUpTypDDWsCT7a3`.
Goal: `goal_33b27643c6b14e65a02ceac7d4d58a1f`.
Workspace: `build/native-927106a-progress-20260913/workspace`.
Criterion: `status.txt contains exactly ready followed by one LF newline (U+000A), no CR`.

The worker started only after native goal attachment. Exact `ready\n` bytes
were present by the 20.7-second sample and remained exact through 321 seconds.
Home showed this worker's own progress, then Stopped / zero Working rather than
the earlier false Working restart. One real Strands review completed with NOOP
and supported file acceptance: one model call, 3307 input/179 output tokens.
All 77 events settled (five pipeline, 72 record-only). Vendor transcript has one
user message: zero follow-ups. Saved vault configuration supplied the actual
inference; no new key entry or fresh-key onboarding claim is made.

Inspector distinguishes supported file acceptance from unconfirmed overall
completion. Ask PEX answered uncertain completion. STOP lineage was contiguous;
the review had no extracted claims (`verification.status=no_claims`), so this
is not evidence of a stream-gap failure. Overall completion reporting still
needs work; do not label the entire goal/UI acceptance perfect.

PEX closed normally, confirmed no desktop process remained. Fixture cleanup
confirms only its owned server exited and profiles were retained.

- Installed receipt SHA256 `cb74d3f57ec0b198bb68e8507e3cafddd155a08c60b94931bf2ae5da105fe955`.
- Vendor messages SHA256 `8150f7662948d93c4dfb540d46033926ef114232ac8dd38471ee26fbf008cc26`.
- Both receipts remain under `build/native-927106a-progress-20260913/`.

## Resource sample, not a lightweight/leak claim

Installed PEX and its descendants only, ten processes, one 30.04-second settled
window: 734.41 MiB summed working set, 382.23 MiB private memory, 19.95% of one
CPU core. Shared working-set pages may be counted twice. This is neither a
long-duration idle baseline nor proof of a leak. Performance remains an open
quality issue. Receipt: `resource-sample.json` in the native evidence directory.

## Real Codex pair: one PASS, one FAIL

Same source, real isolated Codex App Server / gpt-5.3-codex-spark worker and saved
Zen/Muse Strands supervisor. Recovery passed: specific same-thread intervention,
exact shipped file, helped=true, final NOOP. Quiet control failed: Spark wrote
UTF-8 BOM + pong; PEX accepted its weaker `ping.txt contains pong` criterion,
but the independent test rejected the artifact. Pair: one passed, one failed
in 137.34 seconds. Original task, assertion and raw evidence remain unchanged.

Directory: `build/codex-927106a-20260913/`.
Failure XML SHA256 `4c87de83b230bc1652e9dbc73ead958231acdbfb52a6b67821acbb7684b9ed20`.
Validated recovery proof SHA256 `31ba24e5fb32a61c4ab56264a3ddfdd3f9da434e09d28e06db9ce6f8ed54c12d`.
The copied quiet proof is a running/failed attempt, not a validated quiet pass.

## Repair after 927106a

Nine new regressions reproduced that an exact-file objective could be weakened
by a substring acceptance criterion (BOM, extra newline, or prefix). Four controls
already passed. The verifier now retains a narrowly parsed explicit exact-file
objective as an additional check, including `Create/Write/Update ... containing
exactly the word ...`. It reuses whole-string safe-path parsing; it does not
strip BOMs, relax bytes, invent exactness from ordinary contains, or guess
compound prose. Original live Codex prompts/assertions are unchanged.

134 verifier tests passed after repair; expanded cross-layer selection passed
155 tests (3975 deselected) in 21.56 seconds, and Ruff passed. This repair is newer
than the installed 927106a package and needs fresh live and matching package
verification. Prior failed receipts must remain in the failure ledger.

## AgentCore and formal benchmark boundary

183 local AgentCore tests passed in 13.04 seconds on 927106a. AgentCore is not
deployed or AWS-live-proven. At 04:03 UTC formal readiness still returned
can_freeze=false, no coherent runs: missing OS-isolated worker/PEX/hidden-evaluator
backend, immutable complete harness logs, controlled Cursor same-session/network
receipts and all 32 coherent presentation cells; synthetic smoke fingerprint is
also invalid. No headline score or success-lift claim is available.
