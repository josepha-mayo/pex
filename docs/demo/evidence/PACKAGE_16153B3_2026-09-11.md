# PEX storage-safe two-pet package candidate — 11 September 2026

Package source: `16153b34bed8474e08f720ad2b2213cd69c39817`.

This candidate contains the complete exact-Zen package from `f585562` plus one
narrow pet-control resilience repair. Pet visibility now fails open when the
WebView storage API is unavailable, and hide/show still notifies the other PEX
WebView for the current launch even when persistence fails. This prevents a
storage exception from blanking startup or making the pet control appear dead.

## Verification

- The new regression exercises throwing `getItem` and `setItem` operations.
- Focused pet suite: 18 passed.
- Full desktop contracts: 292 passed, one Windows symlink skip.
- Production TypeScript/Vite build: passed, 69 modules transformed.
- Full Tauri release build: passed; MSI and NSIS completed.
- Package verifier: `release_ready: true`, zero blockers, source bound exactly
  to `16153b34bed8474e08f720ad2b2213cd69c39817`.
- Frozen bridge lifetime and standalone two-pet manifest: 3 passed in 11.97
  seconds against the rebuilt runtime. An initial run without the required
  opt-in executable variable skipped all three and is not counted as a pass.
- A fresh isolated settings smoke against the exact packaged one-directory
  bridge (`e68da54a...`) passed. Public HMAC identity and authenticated
  `/v1/supervisor` succeeded; the dispatch cap was `3`, and the first Zen entry
  was `muse-spark-1.3-contributor-free`. Cloud reasoning and worker attachment
  were disabled, no provider call ran, the random bearer was absent from the
  captured log, and the owned Windows job was closed after the check. The
  machine-readable local receipt is
  `build/packaged-settings-smoke-16153b3.json`; the reusable command is
  `scripts/smoke_packaged_bridge.py`.
- The package retains the previously verified exact Zen
  `muse-spark-1.3-contributor-free` Responses route, three-dispatch default,
  Pex/Von-only release inventory, Strands runtime, and local AgentCore path.
  Those backend inputs were unchanged by this frontend-only repair.
- A fresh current-head MVP spine rerun passed 252/252 with unhandled pytest
  thread warnings promoted to failures. It covers goal lifecycle, ten quiet
  completions, recovery continuation, two-pet APIs, authenticated supervisor
  settings, Strands decision policy, Zen provider/Ask routing, and the local
  AgentCore pipeline. It made no live model or AWS call.
- Post-package exact-head checks passed: desktop 292 with one intentional Windows
  symlink skip, production TypeScript/Vite build (69 modules), native Rust 19/19,
  and release pet validation with exactly `pex` and `von`. The first pet
  validation invocation lacked the pinned Rust directory on `PATH` and failed
  before validation; the corrected documented environment passed.

## Immutable artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,726,664 | `f875d97105241c6c6523f9013922aa3bb571faba7954ff4e7fb6748be634bff4` |
| `PEX_0.1.0_x64-setup.exe` | 98,910,483 | `523d36185e8ddd1c32016a50fb3692c6ac47d5a61b8657650b9763bee55a3619` |
| `build/package-16153b3-20260911-verified.json` | 1,548,247 | `c100e58475d8ae6e9910d3b87b56d2f5bde58a1aa20ff1535cb5d1fc0a66bc93` |

The installers remain unsigned candidates. Package integrity is accepted;
visible native UI/UX, foreground/active-worker stability, the final bounded
OpenCode recovery/quiet pair, recording, public release, and submission remain
open. No provider, worker, model, browser, AWS, or native PEX UI ran during this
build and verification; only the isolated headless package bridge ran for the
settings smoke and was stopped by its owned job.
