# Repaired installed OpenCode quiet behavior — 13 September 2026

Installed package: `0ea2639`, desktop SHA-256
`69159786a0ad146513b4c196bb9b8a352b930c4eb35aeaba518ff411ab1503d8`.
Run: `build/native-0ea2639-quiet-20260913`.
Vendor session: `ses_f67b772a0ffejndY2TdeX3f7RL`.
Goal: `goal_2de2ca60cefa46c49f2fa97e14ba0013`.
Native audit SHA-256:
`152255ae6e5827dd78e953e8a40bed0a03d4ade75df85b98c46c4719bc07b218`.

## What was actually exercised

Verified MSI/NSIS bytes were retained and the NSIS installed successfully.
Opened the installed PEX app, inspected the earlier timeout's corrected Review
Incomplete label, connected a fresh owned OpenCode server, selected the exact
fresh workspace, and saved/attached the goal through the native UI.

The objective and all three criteria were unchanged from the failed `32a0499`
run; only the fresh workspace, session and title differed. Worker was again
`ling-3.0-flash-fin-free`; supervisor was saved Zen BYOK
`muse-spark-1.3-contributor-free`, native bridge/local Strands 1.53.0, cap three.
No separate source Pipeline performed supervision. No paid fallback or AWS
deployment was introduced.

## Observed outcome

- Both files matched exact LF bytes by the 32-second sample; worker first idle
  was observed at approximately 46 seconds.
- 138 events settled: 137 record-only complete, one decision complete.
- The completed Strands review used `run_verification` and `inspect_file`, then
  chose NOOP because both files matched and no intervention was needed.
- Review latency 19,261 ms; two model calls; 7,214 input and 1,038 output tokens.
- No independent-verifier timeout or fallback NOOP occurred in this run.
- Zero follow-up/correction deliveries. The owned worker server exited after
  the stop signal; messages, audit, profiles and artifacts were retained.
- Inspector displayed the real quiet decision and 8,252-token receipt.
- Ask PEX's native quick question correctly reported the selected OpenCode
  worker stopped on this persistent goal. It did not interrupt the worker.

**PASS: bounded quiet-supervision behavior**, not verified overall goal
completion or a comparative benchmark result. Deterministic acceptance evidence
now includes both exact-file equality checks, proving the quoted-path repair
was exercised in the installed backend. The third unstructured verification
criterion remains explicitly `unchecked`, so aggregate acceptance and overall
completion remain uncertain. That requirement was not removed to force success.

The prior installed failure remains a failure, preserved separately. These
sequential samples establish useful behavior but do not estimate failure rates
or prove that the code change alone caused all latency differences.

## Remaining observed UX issue

Inspector stayed Discovered during intermediate worker progress, then updated
to Stopped after the decision. This is not claimed fixed. Source inspection
found OpenCode's nonterminal fast path in `pipeline.py::_ingest_event_locked`
durably records/publishes frames but skips the session-status projection used
by the decision path. A follow-up must project authoritative busy/retry state
without turning every message fragment into capability probes or model calls,
and preserve identity, paused-state and event-order guards. Do not remove the
fast path wholesale: that would reintroduce expensive progress-frame planning.

Short native checks do not establish long-run freeze/leak clearance. The formal
four-arm benchmark remains unfrozen; AgentCore is locally tested, undeployed.
