# Installed OpenCode quiet check — failed acceptance, 13 September 2026

Package: `32a0499`; installed desktop SHA-256
`88199799dc2079d617ccbb5b23aa05a589d9a9d3fefa4825ab3b7104171d2f1a`.
Retained run: `build/native-32a0499-quiet-20260913`.
Vendor session: `ses_f67c92453ffeWwm1rRlj4NH7bb`.
Goal: `goal_22d3919662824b9a86840ddd9c14a488`.

## Real path exercised

Installed PEX was opened normally. Through its native UI, connected the owned
OpenCode loopback server on 4097, selected its fresh workspace, entered a title,
objective and exact file criteria, then saved and attached the goal. No separate
source Pipeline supplied supervision. The fixture only operated the dedicated
OpenCode worker; the installed bridge owned review and policy decisions.

Worker: `opencode/ling-3.0-flash-fin-free`; supervisor: saved Zen BYOK
`muse-spark-1.3-contributor-free`, local Strands 1.53.0, review allowance 3.
Official Zen pricing was checked immediately beforehand and listed both routes
free. No paid fallback or AWS resource was used. Only public throwaway content
was sent; credentials were neither printed nor retained in this report.

## Actual outcome

- At the first observed worker idle, about 31 seconds after dispatch, both
  files had their exact required LF bytes (13 and 18 bytes).
- 87 native events settled: 86 record-only complete, one decision complete.
- Real Strands review inspected the workspace and both files. Four model calls
  were recorded; reported usage was 7,207 input and 1,145 output tokens. These
  reported totals may omit tokens from the timed-out verifier request.
- Independent verification timed out after 25,023 ms. Combined review latency
  was 47,102 ms. The final action was fail-closed NOOP with evidence
  `independent_verifier:timeout`, not an affirmative quiet-completion verdict.
- Zero worker corrections were sent. Overall completion remained uncertain.
- Inspector displayed the real usage receipt but called this "Stayed Quiet,"
  which inadequately distinguished a failed review from a successful one.

**Verdict: FAIL for the predeclared quiet-review acceptance condition.** Correct
files and no intervention do not substitute for a completed successful review.
No retry was used to overwrite this result. Native audit, messages, profiles,
progress and the failed outcome remain in the retained directory.

The owned server exited normally through the fixture's stop signal. Profiles
were deliberately preserved; `profile-retention.json` overrides the legacy
cleanup flag, which must not be read as proof that profiles were deleted.

## Root cause and bounded source repair

The exact-content parser rejected Markdown backticks around the filename,
despite that formatting appearing in the recording card. It therefore failed
to recognize either file requirement. Additionally, the third criterion
"Both files are verified before the whole goal is considered complete" is an
unstructured verification requirement and correctly remains unresolved.

The source repair normalizes a valid leading Markdown path span, preserves exact
LF comparison and unsafe-path rejection, and exposes acceptance evidence even
when no completion claim was extracted. It does not discard the third criterion
or turn uncertain completion into success. UI projection now labels independent
verifier timeout/missing-output/failure NOOPs "Review incomplete."

Validation: 96 verifier tests, 300 desktop tests (one Windows symlink skip),
74-module frontend build, and 214 selected recovery/AgentCore/context tests
passed. These source repairs require a rebuilt package and new live acceptance;
they do not retrospectively change this failed installed-package result.

Formal four-arm readiness was rerun and remains unfrozen. Missing coherent
same-session Cursor treatment, immutable raw logs and isolated evaluation mean
there is still no comparative benchmark score to publish.

After the owned worker server exited, a 46.41-second ancestry-scoped sample of
the installed PEX process family on Inspector measured 386.04–395.69 MiB
private memory and 19.90% of one logical core (about 1.66% aggregate on this
12-logical-processor machine). Raw samples:
`build/native-inspector-32a0499-after-quiet.json`. The app remained responsive.
This is a short post-inference sample, not an idle baseline or leak clearance;
other offline checks were running concurrently outside the measured family.
