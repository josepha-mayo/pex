# Current-source offline regression — 8220857

Date: 2026-09-13 (Africa/Lagos)

Source: `82208576ed3c474dd903d24e2d3e7adb04b9c15f`

## Result

The complete non-live Python suite passed from the migrated D-drive checkout:

```text
4532 passed, 16 skipped, 18 deselected in 3020.82s (0:50:20)
```

The retained JUnit receipt is `build/offline-8220857.xml`. Its aggregate is
4,548 collected tests, 0 failures, 0 errors, 16 skipped, and 3,020.672 seconds.
The 18 deselected tests are the explicitly excluded live markers:
`live_llm`, `live_codex`, `live_opencode`, `live_agentcore`, and `live_desktop`.

The run used `D:\PEX-test-temp\offline-8220857-r2` for temporary files and a
fresh D-drive bytecode cache. It therefore did not depend on copied bytecode or
the retired C-drive checkout.

## Repaired preconditions

The first collection attempt exposed zero-filled files in the copied
`opentelemetry-sdk` package. The locked version `1.44.0` was force-reinstalled
into the local virtual environment. A fresh process then imported `strands` and
`opentelemetry.sdk.metrics`, and `pip check` reported no broken requirements.
This was an environment repair only; it did not alter tracked source.

The prior nine deterministic failures were fixture contract drift:

- five fixtures wrote `shipped` plus LF despite an exact-byte `shipped`
  objective; they now write the claimed bytes;
- four timeout tests no longer impose a 100 ms fault-injection timeout on
  connection initialization. Direct-dispatch cases lower the timeout only
  after the verified connection is ready; the claimed-dispatch case retains
  its required authoritative pre-write read.

The four affected files passed together: 114 passed in 148.04 seconds. The
claimed-dispatch loss-of-ack case also passed alone in 3.82 seconds. Production
verification and shared-Codex authority safeguards were not weakened.

## Boundary

This receipt proves the current source's non-live regression gate only. It is
not a fresh installer result, native UI acceptance, live Zen/OpenCode/Codex
run, AWS AgentCore deployment, or formal four-arm benchmark score. Those remain
separate gates.
