# Logged-out public Devpost verification — 13 September 2026

Checked read-only at `2026-09-13T20:22:35Z` without using the submitter's
account or mutating the project.

Public URL: `https://devpost.com/software/pex-mbcpr4`

## Verified public rendering

- HTTP/browser navigation succeeded while logged out and rendered title `PEX`.
- Tagline: `Stop babysitting coding agents. Keep the goals and irreversible decisions.`
- The public story describes the local Windows companion, durable goal,
  evidence-grounded Strands decision, local policy and same-session correction.
- It names OpenCode HTTP and isolated Codex App Server, Zen BYOK, exactly Pex
  and Von, locally tested but undeployed AgentCore, and the unfrozen benchmark
  without an uplift or leaderboard claim.
- The story links the public RC4 GitHub release and the page's `Try it out`
  section links the public repository.
- Created-by attribution renders as Joseph Ayanda.

Public metadata rendered `og:title=PEX` and a Devpost-hosted 333×222 thumbnail.
The thumbnail visibly contains the current simple mint/navy cat mark and check
ring, with no heart motif. This verifies Devpost's processed public derivative;
the canonical local source remains `docs/demo/assets/pex-mark.png`.

## Still absent

The logged-out page did not expose a public demo video or architecture diagram.
No hackathon-submission receipt was observed. The public portfolio project is
therefore useful proof of accessibility, not proof that the required hackathon
entry has been submitted or is complete.

The official rules refreshed the same day still require a public repository
with README and open-source license, architecture diagram, public YouTube or
Vimeo video no longer than five minutes, AWS Builder ID and a working Strands
project. AgentCore deployment and a live-demo URL remain optional.

## Public repository preflight

The GitHub API independently reported `josepha-mayo/pex` as `PUBLIC` with
default branch `main` and detected license `MIT License`. It served both
`README.md` (16,096 bytes) and `LICENSE` (1,073 bytes). Remote `main` and the
clean local checkout both resolved to
`31ee1d78f1b50017a12135209669c7a12c361721` at verification time.
