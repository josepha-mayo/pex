# 23609bc — real recovery, quiet control, installed acceptance findings

Source: `23609bc815c0f619e26f68e5e2e6bbe7ff5b2492` (pushed to main).
Neither source live run changed its source commit/worktree. Real worker:
OpenCode Ling free. Real supervisor: saved Zen Muse Contributor Free via
Strands. No AWS deployment, paid-provider switch, public release or submission.

## Source behavior checks

1. **Controlled recovery passed**, 93.70 seconds. Worker was explicitly asked
   to perform phase one and stop; the persistent goal required both files.
   PEX independently found the missing second file, sent one independently
   approved correction to the same vendor session, observed both exact files,
   recorded `helped: true` / `goal_evidence_supported`, then returned NOOP.
   134 events settled, five supervisor model calls, 16702 input/1923 output
   tokens. Independent verifier took 13.54 seconds; this run does not establish
   that the new 45-second allowance eliminates timeouts.
2. **Correct-completion quiet control passed**, 123.76 seconds. File was exact
   before the first PEX review and input remained unchanged. One successful
   semantic NOOP, no independent-verifier rejection, zero follow-ups, 167
   settled events, two model calls, 6902 input/709 output tokens.

Both owned servers exited; profiles and original evidence remain. These are
two bounded behavioral checks, NOT a comparative benchmark, ten-case rerun,
or native desktop supervision. Earlier ten-case evidence remains on 352d317.

Receipts:

- `build/recovery-23609bc-20260913/summary.json`, SHA256
  `27919f256ed86b4caf97b50758dfa18989f4ea74e095617c3b7ac8d4862790b5`.
- `build/quiet-23609bc-20260913/summary.json`, SHA256
  `1cac523612fc7b085c6398eb9858449c891056585f1d6f6876059c7e759d2705`.

## Package and installation

Full PyInstaller/TypeScript/Vite/Rust/MSI/NSIS build succeeded. Frozen bridge
lifetime: three passed in 12.25 seconds. Package verification initially could
not find Rust in that shell; after restoring the pinned cargo-bin PATH, it
passed with zero blockers. No package failure was waived.

- NSIS SHA256 `9e7503f1ae05b31477b10732f3f3bafc720b427464a5c73118346d203f0cf80e`.
- MSI SHA256 `983cc362ff9b4a997f7f62ce21ed619c0cbe2b3e3c913dd7c8b97dd927bcf755`.
- Retained installers: `build/release-candidate-23609bc/`.
- Receipt `build/pex-package-receipt-23609bc.json`, SHA256
  `d1d7e92397ca8aa820e15d50452168fe92359af21fc75fc9fb0ed0a70d3dd1a8`.
- Installer exited 0. Installed desktop SHA256 exactly matched its extracted
  NSIS counterpart: `67bd964d1e462f2136f759e6be2b5ffdf8600dc5cb51b583acaa587356140729`.

## Actual installed app check

Native input confined to PEX. Startup and both pets' settings worked. Saved
Zen provider, Muse model and three-review allowance remained visible. Connected
the fresh owned server on 4097 through Settings (address defaults to 4096 after
restart; no persistent-connection claim). Selected session and created its goal
through the UI, preserving the explicit LF/U+000A/no-CR criterion from the
earlier failed native run.

Session `opencode:ses_f673850c7ffemfP7AEZTLMMEq8`, goal
`goal_68f456f8dd1e4fcdaf3fa552a1f969a7`. Raw evidence retained under
`build/native-23609bc-progress-20260913/`; runner
`build/native_23609bc_progress.py`.

The selected-worker bubble correctly showed no observed progress before this
worker emitted text, then its actual command error and final response. No
cross-session message fallback appeared. Worker recovered from two transient
provider-unavailable responses, then wrote exactly `ready\n`. Two real Strands
reviews completed with supported acceptance and NOOP, one model call each.
Only one vendor user message existed: **zero PEX follow-ups**. At inspection,
17 pipeline and 212 record-only events were settled. Exact output remained
stable through the retained 271-second sample. Owned worker server and PEX
were closed normally; no unrelated processes were killed.

## Native failures found, not concealed

1. Auto-selected worker changed while goal setup was open, because background
   polling reordered the default selection. Stopped before saving; explicitly
   selecting the intended worker allowed safe attachment. Source now pins the
   current worker on Inspector/navigation entry and does not fall back to
   another worker if an explicit selection disappears. Floating pet keeps its
   fleet-wide status; only Home is selected-worker-specific.
2. After successful completion, Home/Inspector incorrectly remained Working.
   A late `message.updated` for the original user ID arrived after session.idle.
   The stream had a recorded gap, so the strict completed-parent authority cache
   was intentionally empty. That metadata refresh was misclassified as a new
   user prompt. New regression reproduced failure. Source now treats known
   user metadata-only updates as STATUS, preserving actual text/part edits and
   new user IDs as prompts. Stream-gap and outcome-lineage checks are unchanged;
  no historical DB event is rewritten. Initial cross-layer suite: 108 passed.

Final related suite: **239 passed, 3878 deselected in 41.08 seconds**, receipt
`build/opencode-post-native-20260913.xml`. Desktop: 301 passed, one platform
symlink skip; production frontend build, Ruff and diff checks passed.

These last two repairs are newer than installed 23609bc. They require matching
packaging and native acceptance. Therefore this is NOT a perfect-UI or complete
submission-readiness claim, despite the genuine quiet behavior and file result.
