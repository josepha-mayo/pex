# Packaged native OpenCode recovery — 10 September 2026

**Passed bounded same-worker recovery, not submission GO.** Source
`d55e8998ff003730ff0276f720e477a4ba8cc6aa`; later documentation-only commit
`d16dfda` does not change the tested binary.

## Verified chain

The native PEX app opened successfully, loaded saved Zen/Muse supervision,
kept the pet hidden, and connected through its OpenCode settings form to the
dedicated existing local server. The fresh worker displayed Discovered. The
goal editor saved and attached the two-file criteria; a read-only SQLite
check verified exact criteria and evidence requirements before the worker ran.

Local run: `build/native-opencode-d55e899-recovery-20260910`.
Session: `opencode:ses_f761f6c52ffepC2QuYKEKrtIbs`.
Goal: `goal_84597cab1c4f4d30a736e9c4da57941c`.

The initial worker task deliberately completed phase one only. The persistent
goal required both stages. This is a controlled behavioral check, not an
equal-prompt comparative benchmark. The workspace had its own Git root and
explicit path, not an OS sandbox. No hidden evaluator or scripted supervisor
correction was supplied to PEX.

1. `stage-one.txt` became exactly `stage-one-ok` plus newline; `final.txt` was
   missing. First worker idle was about 27 seconds.
2. Native Strands inspected the gap and proposed a specific correction. An
   independent verifier approved it. PEX sent it to the same worker at about
   94 seconds, without a manual corrective prompt.
3. The exact model-authored correction was: "Continue to complete stage two:
   create final.txt containing exactly `pex-supervised-ok` followed by one
   newline, then verify both stage-one.txt and final.txt before stopping."
4. Both files were exact by the 125-second observation.
5. PEX's final review selected NOOP. The original intervention became
   `goal_evidence_supported`, `helped: true`, with the assistant response
   correlated through the native outcome path.
6. At sealing, all 216 events were settled: 139 `complete`, 77
   `record_only_complete`. There were exactly two semantic reviews, not an
   additional review of the buffered old idle event.

The visible Inspector correctly showed **Stopped**, **Stayed Quiet**, the
recorded rationale, and one of three review dispatches remaining. The latest
meaningful-progress label still exposed a terse `user` label from metadata;
this is a presentation limitation, not proof of additional worker activity.

## Provider and cost accounting

Worker: Ling Flash Fin Free. Supervisor: OS-vault Zen Muse Spark 1.3 Contributor
Free, local Strands, saved three-dispatch cap. No paid provider or AWS fallback.

| Review | Model calls | Input tokens | Output tokens | Inference latency |
| --- | --- | --- | --- | --- |
| Correction + independent verification | 5 | 16492 | 1270 | 14458 ms |
| Supported-completion NOOP | 1 | 3193 | 293 | 3145 ms |

The correction cost is reported, not hidden. Dispatch count differs from inner
model-call count. No claim of measured dollar cost is made.

## Package and sealed local evidence

Build and MSI/NSIS extraction/content/eight-pet inventory verification exited 0.

| Artifact | SHA-256 |
| --- | --- |
| Package receipt `build/pex-package-receipt-d55e899.json` | `7fb3569bb79f40b00789df5ff4994839009e0f301cd4f4ee377d375d8435e7fd` |
| Canonical desktop | `aa3d0d1ce3aa1776a452f73226522ca743cfc45ac7ce0be43ccc5bf3226c273b` |
| MSI | `c166513a91f6f921019f1891bdbc2a17992a1d9618955a752c2853d3744840c9` |
| NSIS | `83957d765a10fb0ae90f6a7f606e3db87a96d6669aa48c14793a41d70b68e2f1` |
| Native audit `native-audit.json` in the run directory | `3a9dc742a9da6858b2601cbc2bf4aa2fb0f775c35fad980cea9050b77343fb73` |
| Worker messages `worker-messages.json` in the run directory | `b6bf6a76c816113d0d5051b27b1bba6918937ba3cd27f5658c5449ba40c9eb3d` |

The fixture exited 0 after the sealed result and stopped only its owned server.
Profile cleanup was incomplete: its `data` directory remains. PEX remains open
with the pet hidden. No unrelated Codex/Cursor/Devin app was controlled or closed.
Raw local audit/messages require privacy review before publication.

## Limits and remaining gates

Together with the earlier packaged quiet case, this proves a real native
OpenCode correction/verification/quiet chain. It is not ten varied quiet tasks,
native existing-Codex attachment, long-duration freeze clearance, an OS-isolated
four-arm benchmark, deployed AgentCore, recording or submission acceptance.
Mixed old/new SSE batches intentionally remain conservative and may still
produce a redundant review; the successful case does not prove all timing races.
