# Package and native acceptance — 1cd42c8

Date: 2026-09-13 (Africa/Lagos)

Package source: `1cd42c8790d72035252cf7d1f94654ec4931fbe3`

## Build and package result

The source at `1cd42c8` differs from the fully tested product source at
`8220857` only by the offline-gate evidence and handoff update. The complete
non-live Python suite on `8220857` passed 4,532 tests with 16 skips and 18
explicit live-marker deselections. See
`OFFLINE_GATE_8220857_2026-09-13.md` for that receipt and its boundaries.

The copied D-drive environment was repaired at locked versions after a full
bytecode compile found damaged dependency files. Fresh compilation of all
site-packages, Strands/AgentCore/provider imports, and `pip check` then passed.
These were local environment repairs, not tracked product changes.

Fresh release sidecars, release preflight, production frontend, Rust desktop,
MSI, and NSIS builds completed. Desktop tests reported 302 passed, one Windows
symlink-capability skip, and zero failures. Frozen bridge lifetime/standalone
acceptance reported three passed in 9.78 seconds.

`build/pex-package-receipt.json` is a v2 package receipt for `1cd42c8`. It
reports `release_ready: true`, no blockers, verified MSI and NSIS inventories,
and canonical desktop SHA-256
`5c37d2940b852ae97a49e1f33d8c4bf8fa6bc7438fa4b60a4e6eedc5da55eae2`.
The retained candidate is `build/release-candidate-1cd42c8`:

- `PEX_0.1.0_x64_en-US.msi` — 114,499,608 bytes — SHA-256
  `7254c5aaf88150bd156f33cb24dc01dc380d6273252cda5c85a1194e87191663`
- `PEX_0.1.0_x64-setup.exe` — 101,689,109 bytes — SHA-256
  `81c85b528e0e7aeae57a6c4bc554b08c54302df87f776f034bbec7aa9f36d849`
- `pex-package-receipt.json` — 1,546,516 bytes — SHA-256
  `40cd6d6b244a04d6709d3676d81b761139addec74649e5f51383f746b41bd602`

The NSIS installer exited zero. The installed
`C:\Users\JosephMayo\AppData\Local\PEX\pex-desktop.exe` resolves through the
approved D-drive junction and hashes to
`80e35b4f6231956cf47bd31272fbec482d791082f0130173da29f0471240d4b2`, exactly
matching the NSIS receipt's embedded desktop hash.

## Native acceptance on the installed candidate

PEX opened without a bridge error and owned exactly one desktop process plus
one packaged bridge process. The bridge listened on its assigned loopback port
7420, and unauthenticated `GET /health/live` returned
`{"ok":true,"service":"pex-bridge"}`.

PEX-only native inspection confirmed:

- Home, Inspector, Deck, and Settings are keyboard-accessible application
  surfaces; Home remained responsive during the check.
- Companion settings display exactly two built-in companions: Pex and Von.
- The selected Von companion is rendered on the app stage without the former
  opaque white card around the artwork.
- The status message has an independent accessible dismiss control. Activating
  it removed the message while the Von companion remained present.
- Supervisor settings display the saved limit of three semantic dispatches per
  session and explicitly distinguish dispatches from dollars, tokens, or calls.
- The saved provider is OpenCode Zen, the model id is
  `muse-spark-1.3-contributor-free`, authentication is `api_key`, and the
  endpoint is `https://opencode.ai/zen/v1`.
- The API-key input is write-only. The UI reports that a credential is stored
  in the OS vault; neither the value nor its reference was read or printed.
- Connections provides the local OpenCode server flow and a separate existing
  Codex observer flow. It truthfully reports that no shared Codex observer is
  currently recorded by this bridge.

The migrated local database contains eight historical worker sessions. The
Home rail therefore says `8 AVAILABLE`; this is a worker-session count, not a
pet count. It also retains an old C-drive workspace path for a historical
OpenCode session. A clean second-laptop install will not inherit this migrated
history.

A bounded 20-second process sample while the Home window was visible observed:

- packaged bridge: 0.06 CPU-seconds, 142.8 MiB working set, 121.8 MiB private;
- native desktop: 0.16 CPU-seconds, 35.3 MiB working set, 6.7 MiB private.

This is a bounded idle sample, not a long-duration leak or performance proof.

## Remaining proof boundary

This checkpoint proves build/package integrity, installed binary identity,
native startup, the specific two-pet/settings/message-dismiss UX, bridge
liveness, and a bounded resource sample. It does not prove a fresh current-build
Zen inference, OpenCode recovery/quiet pair, Codex recovery/quiet pair, live AWS
AgentCore deployment, or a frozen four-arm benchmark score. Historical live
receipts remain version-bound. Those gates remain open and the product goal
remains active.
