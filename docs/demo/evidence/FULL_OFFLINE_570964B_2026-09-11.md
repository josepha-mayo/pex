# PEX full offline regression — 11 September 2026

Source: `570964b1eb61ba220aa0edbff3e25a3952994ab6`, with a clean working tree and
`HEAD == origin/main` before the run.

The pinned Rust 1.97.1 toolchain directory was prepended to the command's PATH,
then the complete Python suite ran with unhandled pytest thread exceptions
promoted to failures:

```text
.venv\Scripts\python.exe -m pytest -q -W error::pytest.PytestUnhandledThreadExceptionWarning --tb=short
```

Result: **4,440 passed, 32 skipped in 2,342.58 seconds (39:02), exit 0**.

An immediately preceding run from the ordinary shell finished with 4,439 passed,
32 skipped and one failure: release preflight returned an incomplete JSON object
when `rustc` was absent from PATH. Source `570964b` repairs that fail-closed path:
preflight now keeps the full fleet, Git, sidecar and toolchain audit, records
`rust_toolchain_unavailable` as a blocker, and never claims source readiness.
The exact failed node then passed independently without Rust on PATH, the complete
desktop suite passed 290 with one intentional Windows symlink skip, and the clean
full run above passed with the pinned toolchain available.

This is offline regression evidence. It made no model-provider, worker, browser,
native PEX-window or AWS call, and it does not replace the remaining packaged
native UX and live OpenCode supervision gates.
