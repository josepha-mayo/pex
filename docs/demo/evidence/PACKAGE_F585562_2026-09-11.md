# PEX exact-Zen package candidate — 11 September 2026

Package source: `f5855620fa2e2bf944b10cbd17382741cf121adb`.

This source corrects the last inconsistent Zen path. The normal Strands
supervisor already routed Muse models through the Responses API, but the
read-only Ask PEX fallback still used Chat Completions and silently tried other
Zen model IDs. It now:

- routes the three explicit Muse IDs through `/v1/responses`;
- selects `muse-spark-1.3-contributor-free` as the first Zen static hint and
  fresh-install default;
- sends only the exact operator-selected model;
- permits at most one transient retry of that same model, never a hidden model
  switch;
- rejects malformed response output and tool calls in a read-only review.

## Verification

- Ruff passed on every changed Python and test file.
- Provider/BYOK/settings/security slice: 197 passed.
- Desktop contracts: 291 passed, one Windows symlink skip.
- Production TypeScript/Vite build: passed.
- Full Tauri build: passed with MSI and NSIS artifacts.
- Package verifier: `release_ready: true`, zero blockers.
- Frozen bridge lifetime and standalone manifest: 3 passed.
- Expanded current Strands/supervisor/trajectory/evidence/outcome gate with
  aiosqlite thread warnings promoted to errors: 387 passed, 6 skipped in
  121.17 seconds.
- Current local AgentCore implementation gate under the same warning policy:
  183 passed, one opt-in live-cloud test skipped in 17.05 seconds; no AWS call
  or deployment occurred.
- Current benchmark/Cursor/audit/execution-safety/scoring integrity gate: 368
  passed in 820.73 seconds. One aiosqlite thread-shutdown warning appeared in
  that broad mixed suite; the exact named test then passed with thread warnings
  promoted to errors, and the complete owning Cursor contract passed 57/57
  under the same warning-as-error policy. The warning did not reproduce, so no
  speculative product change was made.
- Isolated packaged bridge smoke: public identity passed, authenticated
  supervisor settings reported the fresh-install dispatch cap of 3, and the
  first Zen hint was `muse-spark-1.3-contributor-free`. No provider, worker, or
  AWS call ran. No bridge process remained afterward.

## Immutable artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,730,760 | `a8b4e2226aa32484aeb8a4799993ec699ed553d07ada58132a3c0322499496ec` |
| `PEX_0.1.0_x64-setup.exe` | 98,909,861 | `be3770f95257cf273b0055503dff35b9e003739610e045beaff1cd3e737cbe85` |
| `build/package-f585562-20260911-verified.json` | 1,548,247 | `0cb41ee1b7d1b1302142ac19107bde9cf157b06ea22517257105daf0fb9ed49b` |

The installers are unsigned development candidates. Visible native UI/UX
acceptance, the final live Zen + Strands + OpenCode/Codex story, fresh recording,
and submission remain open. The benchmark manifest remains `frozen: false`; no
comparative productivity claim is made.
