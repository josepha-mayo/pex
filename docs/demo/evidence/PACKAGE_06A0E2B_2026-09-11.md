# PEX two-pet MVP package candidate — 11 September 2026

Package source: `06a0e2b2d180b75dfe8ece27c9f55583a282b499`.

This candidate contains the complete structured-preflight repair from `570964b`
plus evidence-only documentation. It ships exactly Pex and Von, routes the exact
Zen Contributor Free model through Responses, defaults fresh installs to three
semantic review dispatches per session, and excludes the retired hatch/image
implementation from active startup and the frozen runtime.

## Verification

- Immediate product parent `570964b` passed the complete Python regression:
  4,440 passed, 32 skipped in 2,342.58 seconds, with unhandled pytest thread
  exceptions promoted to failures. Commit `06a0e2b` adds documentation only.
- Complete desktop contracts passed: 290 passed, one intentional Windows
  symlink-permission skip. TypeScript/Vite production build passed, 69 modules.
- Clean Tauri release build passed. The bridge and both helper executables were
  freshly frozen; Rust release compilation finished without warnings; MSI and
  NSIS bundling both completed.
- Package verification completed with `release_ready:true`, zero blockers, and
  source bound exactly to `06a0e2b2d180b75dfe8ece27c9f55583a282b499`.
- Frozen runtime: 2,372 files and zero paths matching
  `pex_bridge/pets/{hatch,hatch_store,imagegen}`.
- Three sequential isolated packaged-bridge restart smokes passed. Each proved
  HMAC identity, authenticated supervisor read, dispatch cap `3`, and first Zen
  catalog entry `muse-spark-1.3-contributor-free`. Cloud reasoning and worker
  attachment were disabled; provider calls were `0`; no bridge survived.

A separate verifier invocation intentionally retained a fail-closed diagnostic:
without Rust on PATH, the repaired preflight emitted its complete audit and
reported `rust_toolchain_unavailable`. A later verification validated both
installers but retained `package_cleanup_failed` after Windows returned `EPERM`
while removing its temporary extraction tree. The successful receipt below was
created by an earlier complete verifier pass over the same immutable artifact
hashes; neither failed attempt overwrote it.

## Immutable artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,644,696 | `0d7083418eea9a662333a64e1c1c3c5ec284a566df1619b6b03f00b5ee66ba1c` |
| `PEX_0.1.0_x64-setup.exe` | 98,830,453 | `4df2b82465d530f904196a20dc7655a6359881dc2fe0c585fe275c7445302e1f` |
| `pex-bridge.exe` | 34,459,441 | `fdf48337e4e24af26698a9420dea12a905adc571301b58f93fafaffdde6eabbf` |
| `build/pex-package-receipt-06a0e2b.json` | 1,546,513 | `8e59ae84fbd6ca8ad34051d55a74cb2b720b628b37efdf199d88b4e18d3dc00b` |

The installers are unsigned candidates. Package integrity and headless bridge
startup are accepted. Native UI/UX, foreground/active-worker stability, the final
bounded Zen/OpenCode corrective and quiet journeys, recording, public release,
and submission remain open. No visible PEX window, browser, live model, worker,
or AWS resource ran during this package gate.
