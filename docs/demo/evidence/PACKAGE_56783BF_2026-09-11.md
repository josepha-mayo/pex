# PEX two-pet recording candidate — 11 September 2026

Package source: `56783bff3dec8c25e725c3dded603b8ad1d76079`.
The product UI change is `8394b4b5cbaf154e6a1aafa29193497957f1d0c2`;
the intervening commit records the two-pet audit.

## Package result

The clean release preflight reported `source_ready: true`, no blockers, exactly
Pex and Von, current sidecars and verified pinned toolchains. The full Tauri
build completed with exit code 0 and produced both Windows installers. The
package verifier then extracted MSI and NSIS, compared the desktop binary,
Cursor helpers and complete bridge runtime, and reported
`release_ready: true` with no blockers.

| Installer | Bytes | SHA-256 |
| --- | ---: | --- |
| `PEX_0.1.0_x64_en-US.msi` | 111,738,952 | `c073a03dc1c0bd6b5b2929af2a945c1a682386bb55b8d04a63dac6a4984eebe9` |
| `PEX_0.1.0_x64-setup.exe` | 98,908,592 | `9e22d8a50fc8bfb792c918e81ef51acc75a2ccf4928400c0f317fa13e5aef53d` |

The local machine-readable receipt is
`build/package-56783bf-20260911-verified.json`, SHA-256
`cacf98bccd87f14673961d1479f13ee0aa791bbbade0f47364695e76f8393e64`.
The installers are unsigned development candidates, not a published release.

## Product and two-pet gates

- Pex and Von are the only release pets. Their exact RGBA WebP v2 atlases pass
  the hatch-pet and runtime contracts with zero errors or warnings.
- The overlay sprite, actor hit area, remaining status-bubble width and hide
  control share one scale-derived width across the supported 0.8–1.4 range.
- Desktop: 291 passed, one platform-specific skip; production build passed.
- Fresh installs default to three durable semantic supervisor dispatches per
  worker session; explicit Settings/environment override remains available.

## Packaged bridge smoke

The frozen bridge from this package was launched headlessly with a fresh
isolated `PEX_HOME`, a random bearer, cloud reasoning disabled and no dispatch
cap override. Public liveness became ready and authenticated
`GET /v1/supervisor` returned `max_dispatches_per_session: 3`. The supervisor
was disabled for this configuration-only smoke, so no provider, worker or AWS
call ran. The owned bridge process tree was stopped; a subsequent process query
found no `pex-bridge.exe`. Logs and isolated state remain under
`build/packaged-cap-smoke-6ecf0ed632234017b57a8b0e50215541`.

The first harness attempt used PowerShell's reserved `$HOME` variable and made
no launch. The second omitted the mandatory frozen-desktop parent PID, so the
bridge correctly failed closed with exit 2. Neither is reported as a product
pass; the corrected run above exited 0.

## Claim boundary

This package does **not** yet prove a fresh-user install, visible native
verification of the new scale-aware overlay/Ask/cancellation behavior,
long-running foreground stability, AgentCore deployment, a frozen comparative
benchmark score, a recorded demo or a Devpost submission. Those remain explicit
shipping gates.
