# Installed PEX recovery — 13 September 2026 (Lagos)

The installed `103f4ec` desktop completed a fresh, controlled OpenCode recovery.
This is a native behavioral check, not a comparative benchmark score.

## Native chain

- Loaded Supervisor settings: saved Zen, exact
  `muse-spark-1.3-contributor-free`, OS-vault credential, three-dispatch cap.
  No credential was re-entered or exposed; this checks saved BYOK use, not a new
  key-entry test.
- Connected through the native Connections form to an owned OpenCode server on
  loopback port 4097. A fresh worker appeared in Home.
- Selected that worker and used the native goal editor to save the two exact
  file requirements. Read-only SQLite inspection confirmed the saved values.
- The worker completed only phase one. At 24 seconds `stage-one.txt` was exact
  and `final.txt` absent. Initial idle was about 36 seconds.
- Native Strands independently reviewed the gap. Its verifier approved one
  `SEND_NUDGE`; the same worker created the second file. Both exact byte checks
  were true by the 86-second observation.
- Final model-backed review selected `NOOP`. The correction recorded
  `goal_evidence_supported`, `helped:true`, and correlated worker response.
- All 163 events settled: 161 record-only complete and two complete semantic
  reviews. One of three dispatches remained. No manual corrective prompt.

| Review | Model calls | Input tokens | Output tokens | Inference latency |
| --- | ---: | ---: | ---: | ---: |
| Correction and independent verification | 4 | 13075 | 1333 | 22252 ms |
| Supported-completion NOOP | 1 | 3305 | 487 | 7529 ms |

Worker: `ling-3.0-flash-fin-free`. Both exact model IDs remain listed as free on
the [official Zen page](https://opencode.ai/docs/zen/), checked before this run.
Only the public throwaway task was sent. No paid fallback or AWS deployment.

## Retained local evidence

Run directory: `build/native-rc2-20260913-recovery-b`.
Vendor session: `ses_f6824ad61ffev507GZJM4sDEZ0`.
Goal: `goal_bd4271c328444709b0ecf4ff998d34a7`.
`native-audit.json`, `worker-messages.json`, `ready.json`, `progress.json`, and
the workspace remain local for inspection. The first unsuffixed directory
retains an initialization failure: the system Python lacked project packages;
no provider call or worker server started in that attempt. The second run used
the project virtual environment.

The fixture exited zero and terminated only its owned server. Its legacy
`cleanup.json` profile-deletion flag is **not applicable**: the wrapper disabled
profile deletion deliberately, so all profiles remain retained. Do not quote
that field as proof of deletion. The fixture's legacy worker title contains
`d55e899`; that is only a title, not the package source.

## Native findings and repair

Inspector showed Stopped and Stayed Quiet with the real rationale, but falsely
said “No model call recorded.” The backend had the correct five-call audit.
Root cause: `actionForSession` omitted inference usage when converting an
intervention into Inspector's display object. The repair preserves model,
provider, call count, tokens and `used_llm`, and only uses fallback evidence
when both session and action IDs match. Missing evidence is now described as
unavailable, not zero calls. Regression tests exercise this exact native
receipt and cross-action isolation. Desktop suite: 297 pass, one Windows
symlink skip; production TypeScript/Vite build passes.

The repair is not yet installed or visually verified. The native result above
belongs to `103f4ec`, not a future corrected package. Goal creation also exposed
a long form with its save button below the fold; that UX issue is not fixed by
the receipt repair.

One process sample showed bridge 162.4 MiB working set and desktop 36.0 MiB;
these exclude WebView subprocesses and are not a total-memory or freeze test.
No sustained memory clearance, fresh Codex run, formal PexBench score, deployed
AgentCore, recording, or submission completion is claimed.
