# Native OpenCode quiet completion and pet checks — 10 September 2026

Source: `490b82ba786eace8bcac5a25e97565b09761304b`. Package and individual
behavioral evidence only; not submission GO or a comparative benchmark.

## Package

`npm run tauri -- build` and package verification exited 0. Both MSI and NSIS
extracted content and eight-pet inventories verified. Local receipt:
`build/pex-package-receipt-490b82b.json`.

| Artifact | SHA-256 |
| --- | --- |
| Canonical desktop | `70a93ce7c13af16febf524d23d3a920b9cf9957503ccd7c731731d53158e78a2` |
| MSI | `442b1dff3aa4c916694c6d328c926654738b1186f8a766e7c9f768218f27c05f` |
| NSIS | `8e9cfca01f11f24c053066c5e5eaa82eb689886c049c008f523cc673e2c5ba1d` |
| Embedded bridge | `435a5b4dee3973bb8d205cc69d90690214bbdfc8f7b6a6c557c051d405ee85a8` |

## Real native quiet case

Local run: `build/native-opencode-490b82b-quiet-20260910`.
Session: `opencode:ses_f7644c92cffeDjyXbCpUIoM6nQ`.
Goal: `goal_d8c2fb1da0d1491e82b3d3af043307ef`.

The native Settings connection and goal editor attached the fresh worker and
exact criteria. The fixture has its own nested Git root and an explicit target
workspace in the initial task. This is directory scoping, NOT OS isolation.
Only the desktop-owned bridge supervised; no second Pipeline was used.

The Ling Flash Fin Free worker created `stage-one.txt` containing `stage-one-ok`
plus one newline and `final.txt` containing `pex-supervised-ok` plus one newline.
Both were exact by about 31 seconds. Provider retries occurred, followed by a
genuine final assistant stop without an error; first idle was about 74 seconds.

By about 170 seconds all 336 events settled: 154 `complete`, 182
`record_only_complete`, none pending. Exactly one semantic review used the saved
OS-vault Muse Spark 1.3 Contributor Free route and local Strands. It returned
NOOP/noop, with acceptance `supported`, one model call, 3195 input and 520 output
tokens, and 8148 ms inference latency. The audit recorded state, recent-event,
and workspace inspection and both file sources. Verification status `no_claims`
means there was no separate parsed claim; it does not negate the independently
supported exact acceptance criteria. No manual nudge or paid fallback occurred.

Observation continued to about 394 seconds without another action, then the
fixture was stopped. Its owned server exited, driver exit 0. Cleanup receipt
reports `profile_cleanup_complete: false`, remaining directory `data`; do not
claim complete profile cleanup or delete evidence through broad cleanup.

| Sealed local receipt | SHA-256 |
| --- | --- |
| `native-audit.json` | `ffa4e3d5f0f2ee23c196cc0e9f222bf301df6f6b40c438db43ba44e2799bb595` |
| `worker-messages.json` | `3ef24abe50882b5961813ad36780cfa0e2d63e4822292cc26052d95226be9545` |

Raw local receipts need privacy review before publication.

## Native UI and short resource observation

Inspector visibly showed Stayed Quiet, but bookkeeping incorrectly restored a
Working badge. Follow-up adapter/pipeline regressions address that projection.
The Inspector follow-up also prefers the recorded action rationale over a raw
diagnosis identifier, without adding polling or derived state effects.

Settings displayed all eight companion thumbnails. Showing Von produced a
transparent laptop-cat pet with the underlying app visible, no white rectangle.
Bubble dismissal worked. Hide Pet closed the pet window, and Settings reflected
the unchecked preference. Pet remained hidden afterward. Close control stayed
at the same local position across the observed animation frames. This was a
short visual check, not a formal motion/framerate benchmark.

A read-only 10.02-second process sample with pet hidden measured about 428.4 MiB
total private memory across the exact PEX process and measured descendants,
and 1.532 CPU seconds (about 15.3% of one core). Bridge Python used 0.047 CPU
seconds and 166.5 MiB private memory. These are a short settled sample, not a
long-duration stability or freeze-cause clearance. No process was terminated by
the measurement script; summed working sets were not treated as exclusive RAM.

## Follow-up verification and remaining work

Stopped-state and metadata-parent regressions passed with the existing delta,
event, pump and lineage group: 114 passed in 56.19 seconds. Ruff initially found
two long test lines; formatting followed by Ruff passed. Desktop status-rationale
change: `npm test` passed all 274 tests; `npm run build` exited 0. These changes
are subsequent source, not part of the native binary above.

The older `2174ad0` failed diagnostic was closed normally after sealing its audit
at 611 rows, including 464 deltas and one planning row. It did not drain fully
and was not a successful quiet/recovery case. Its receipts remain preserved.

Still required: rebuilt-native recovery with correlated outcome; old-buffered-
idle duplicate review repair; native existing-Codex attachment; false-test and
uncertain-claim cases and ten-case false-positive measurement; longer stability;
OS-isolated comparative benchmark; deployed AgentCore proof under verified
no-billing authority; recording and submission review. No gate is waived here.
