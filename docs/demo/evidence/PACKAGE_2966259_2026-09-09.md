# Windows package checkpoint: 2966259

Source: `2966259676113bb05638b501188d53e65f83e66e`, clean at build and verification.

Commands from `apps/desktop`, with pinned Rust 1.97.1 on PATH:

- `npm run tauri -- build` with `CARGO_BUILD_JOBS=2`: exit 0, three sidecars,
  production frontend, native executable, MSI and NSIS built.
- `npm run verify:package -- --receipt C:\Users\JosephMayo\Projects\pex\build\pex-package-receipt-2966259.json`:
  exit 0; both installers verified, exact eight-pet inventories, no blockers.

First verification attempt stopped at source preflight because its shell lacked
the pinned Rust PATH (`rustc ENOENT`). Re-running with the correct PATH passed;
there was no source or package modification between those attempts.

SHA-256:

| Artifact | Hash |
| --- | --- |
| Canonical desktop | `b7f957932e75f099609c20f79e2edb503575278ffd1578266886b1cdd0c7548d` |
| MSI installer | `bfdcd97f9fab2d03a0b676c41b9b7c86f0d830b0621f8da3d993cc9c9be2a11d` |
| NSIS installer | `64210499d4e835c5460a67bea7f3632afb74efc2d48b8680b78e99a14856b02a` |
| Bridge in both installers | `a47aa9faa96a4b19efece9db833f676e6a94f0f04a835e735d97c605a0f7fad7` |
| Normalized desktop bundle variants | `781f25e1f23f3c51a2688acdc2c120db7e8b508e2c41fc9b987504d386010fdd` |

Desktop variants differ only at the expected four-byte Tauri bundle marker.
The receipt retains every helper's size and hash. Bridge size is 102,302,289 bytes;
desktop size is 14,381,056 bytes.

Build warnings included optional Cedar handler dependency, hidden imports
`pycparser.lextab`, `pycparser.yacctab`, `tzdata`, and unused Rust
`bridge_port_state_at`. They did not prevent the checks above. This is not proof
that every optional runtime path works.

Verification used MSI administrative extraction, NSIS archive extraction and the
frozen bridge's `--verify-bundle` inventory-only entry point. It did not install
PEX, launch its desktop UI, attach a worker, or invoke a model/cloud service.
Temporary verifier extraction files were removed by its scoped cleanup.

This candidate includes the pause, BYOK confirmation and trajectory-order fixes
absent from the 493aec0 package. Package `release_ready:true` is only the package
gate: overall submission remains NO-GO pending live UI/resource, BYOK/worker,
quiet-sample, benchmark, AgentCore evidence and submission gates.
