# Rendered production UI preflight — package source 392d86e

Date: 12 September 2026

Source under review:

`392d86e910fc8b969eae82140c8f57b930d8f17d`

This is a production-browser render preflight for the exact source used by the
current MSI and NSIS candidates. It is not a substitute for the native Tauri
acceptance card.

## Result

The production Vite output was served at `http://127.0.0.1:4173` and inspected
through a PEX-only browser surface. The following routes rendered without visible
horizontal overflow, detached controls, overlapping headings or inaccessible
primary navigation at the observed 1280 by 720 viewport:

- Home (`#compact`): explicit OpenCode and Codex harness rail, goal-first main
  panel, one central Pex companion and honest bridge-offline loading copy.
- Inspector (`#inspector`): readable no-worker state, current-agent and action
  cards, persistent-goal form and Ask PEX surface.
- Deck (`#deck`): stable left navigation and the intended 3/2/1 attention-metric
  layout; no recurrence of the prior right-edge overflow.
- Settings / Supervisor: readable review-limit and BYOK guidance, disabled
  controls while canonical state is unavailable, and no credential value shown.
- Settings / Companion: scale, show/hide, click-through and the two-companion
  roster remain clearly separated.
- Settings / Connections: OpenCode loopback setup is the first connection path;
  Codex observation and scoped-hook controls remain visibly distinct.
- Settings / Goals: honest empty state with no fabricated goal.
- Pet route (`pet.html`): one anchored `× Hide` pill and one status bubble beside
  the companion. Dismissing the status bubble removed it independently and left
  the pet plus `× Hide` control in a stable position.

The live installed bridge independently returned HTTP 200 from `/health/live`
while exactly one `pex-desktop` and one `pex-bridge` root process were present.
The persisted pet settings select `von`; the current bundled Pex and Von atlases
remain the only shipping sheets. Von's atlas was inspected directly and retains
its dark-navy cat identity, transparent cells, work/laptop frames and complete
motion/emotion families.

The persisted supervisor record identifies provider `zen`, model
`muse-spark-1.3-contributor-free`, `api_key` authentication, `secret_store`
credential source, a present opaque secret reference and dispatch-limit override
`3`. No secret value was read or printed, and no provider call ran during this
preflight.

## Boundary

The browser surface cannot prove Tauri transparency, always-on-top behavior,
native hide/show, drag-without-activation, native motion cadence, click-through,
or close-time owned-process cleanup. Those checks remain on
`docs/demo/NATIVE_ACCEPTANCE_392D86E.md`. The browser route also cannot obtain the
desktop-owned in-memory bridge bearer by design, so its canonical-data controls
correctly remained unavailable. That security boundary was not bypassed.

